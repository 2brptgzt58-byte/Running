# v7.1 검증 결과

## 수정한 결함
1. hidden 속성을 stack/switch-row CSS가 덮어쓰는 표시 오류.
2. 클릭 저장이 기본 폼 검증을 우회하는 오류와 Enter 제출 경로 불일치.
3. 존재하지 않는 날짜가 다음 달 날짜로 처리되는 오류.
4. 대회 후 회복 기간이 끝난 뒤에도 본운동을 제한하는 조건 오류.
5. 5/10km 대회에서 설정한 테이퍼 기간 밖의 본운동까지 줄이는 오류.
6. 테이퍼 예산을 적은 훈련일에 재분배해 개별 세션이 오히려 길어질 수 있는 문제.
7. 주 경계의 수동 품질훈련 완료와 기록 없는 완료 부하 누락.
8. 코칭에서 강도를 낮춰도 오늘 주간 표시가 기존 품질 유형으로 남는 문제.
9. 숫자·날짜가 잘못된 백업 및 일부만 저장되는 복원 실패 처리 부족.
10. 초 반올림 및 분만 비우고 초를 남긴 입력의 처리 오류.

## 실행 결과
- 4개 종목 × 대회 상대일 16개 × 능력값 4개 × 이력 조건 2개 = 512개 조건 통과. 계획 숫자 유효성, 음수 시간 여부, 예산 준수, 일반 모드 대회 독립성을 확인.
- 별도 회귀 검사: VDOT·5/10km 기록·역치 심박 반영, 통증 우선, 네 종목 테이퍼, 대회 전날/당일/회복, 능력 수동 유지 통과.
- DOM 모형에서 앱 초기화·render·저장 이벤트 실행. 대회 모드 제목, 확인/취소/해제, 입력 검증, 저장 실패 시 값 유지, 재로드, 백업 검증/롤백, Enter 제출, 기존 소수 분 편집 보존 통과.
- JavaScript 문법·HTML ID 연결·숨김 CSS 규칙 검사 통과.

실행: node tests/coaching.cjs / node tests/settings.cjs / node tests/regressions.cjs

## 남은 한계
실제 브라우저 실행 파일 다운로드가 시간 초과로 실패했다. DOM 모형 검사는 실제 Safari의 레이아웃·네이티브 숫자 휠·서비스워커 업데이트를 검증하지 않는다. iPhone에서 최종 확인이 필요하다. 버그가 전혀 없음을 보장하는 검사는 아니다.

목표 달성 퍼센트는 실제 성공 데이터로 보정되지 않은 모형 추정치다. 테스트 통과는 코칭의 생리학적 정확성이나 개인별 효과를 입증하지 않는다.
